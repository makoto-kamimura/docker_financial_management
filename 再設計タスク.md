# 再設計タスク一覧（実装指示書）

本書は [`再設計仕様書.md`](./再設計仕様書.md)・[`再設計詳細設計書.md`](./再設計詳細設計書.md) に定義された残タスクを、**AI モデル・開発者がそのまま着手できる単位**に分解したものである。各タスクは独立して実装・レビュー・マージできるように切ってある。

- 作成日: 2026-07-13
- 対象ブランチ: `claude/redesign-spec-security-88lpk6`（またはそこから派生した作業ブランチ）

---

## 0. 共通実装規約（全タスク必須）

作業前に必ず以下を守ること。

| 規約 | 内容 |
|---|---|
| API 実装 | 新規・変更する Route Handler は必ず `lib/api-handler.ts` の **`withApi()`** で実装する（`role` / `schema` / `querySchema` を宣言、エラーは `lib/api-error.ts` の `notFound()` / `badRequest()` / `conflict()` / `ApiError` を throw）。`requireRole()` の直接呼び出しは新規に書かない |
| テナント分離 | tenantId 列を持つモデルを schema.prisma に追加したら、`lib/tenant-db.ts` の `TENANT_SCOPED_MODELS` に**必ず登録**する。登録漏れは `src/lib/tenant-db.test.ts` が検出して CI が失敗する |
| 計算ロジック | 集計・計算は `src/lib/*.ts` の**純関数**として実装し、`*.test.ts`（Vitest）を必ず併設する。ルートハンドラには I/O と組み立てだけを置く |
| 会計期間・科目解決 | `lib/period.ts` の `resolvePeriod()` / `findAccountByCode()` / `requireAccountByCode()` を使う（手書きの `period.upsert` を新設しない） |
| マイグレーション | `prisma/migrations/<UTC タイムスタンプ>_<名前>/migration.sql` を手書きで追加（この環境では DB 起動不可のため `prisma migrate dev` は使わない）。既存 SQL の命名規約（`"テーブル名_pkey"` 等）に従う。`npx prisma validate` を通すこと |
| 検証 | `cd app/web && npm run typecheck && npm test && npm run format:check && npm run build` がすべて成功してからコミットする |
| 依存パッケージ | 新規依存の追加は原則禁止（xlsx は削除済み。復活させない）。必要な場合はタスク内で理由を明記 |
| ドキュメント | 実装完了時に `再設計仕様書.md` の該当行を【実装済み】へ更新し、本書のタスクのチェックボックスを埋める |

### 完了済み（参照実装）

新タスクの実装時は以下を手本にすること。

- [x] API 共通基盤 `withApi()` と全ルート移行（`lib/api-handler.ts` / `api-error.ts` / `period.ts` / `zod-helpers.ts`）
- [x] xlsx（Excel 取込）依存の削除
- [x] 配分ルールのサーバー側マスタ昇格 — `allocation_rules` テーブル / `GET PUT /api/allocation-rules` / `lib/default-allocation-rules.ts`（seed）/ モバイル `loadAllocation()`・`saveAllocation()`
- [x] テナントスコープ網羅の回帰テスト（`src/lib/tenant-db.test.ts`）
- [x] 予算オーバーレイ計算の共通化（`lib/budget-overlay.ts`）— F-2 / F-7 で再利用する
- [x] SRP リファクタリング第 2 弾 — 消込仕訳 `lib/settlement.ts`（F-5 の転記でも参考になる）/ 仕訳 include・実績連動 `lib/journal.ts` / 銀行明細 upsert `lib/bank-transactions.ts` / テンプレート共通部 `lib/journal-template.ts` / 予測精度・DB enum 写像を `lib/forecast.ts` へ移設 / カテゴリ・グループ定数の一元化（`lib/account-category.ts` ほか）
- [x] S-1: 証憑配信の認可（`lib/upload.ts` 検証込みで先行実装）
- [x] S-4: CSRF 防御とセキュリティヘッダー
- [x] F-1: 収支マイナス警告
- [x] F-4: モード別表示名の全画面反映（Web 全画面 + モバイル `displayName()` 複製）
- [x] F-2/F-3: 配分提案・適用 API + Web 配分提案タブ・予算配分ルールタブ + モバイル「予算に反映」
- [x] S-2: MFA の 2 段階化と総当たり対策（`mfa_challenges` テーブル・`POST /api/auth/mfa/verify`）
- [x] S-3: セッションのハッシュ保存・二重期限・Bearer ログアウト修正
- [x] F-5: 明細と科目の紐付け・実績転記（linkedAccountId は未実装。既知の簡略化として本文に記載）
- [x] F-6: 月間実績フロー図 GET /api/cashflow/monthly
- [x] F-7: 元利均等返済の自動計算とオーバーレイ全ローン対応（number ベース）
- [x] F-8: 資産カテゴリ拡張（CASH/DEPOSIT/SECURITIES）と総資産（純資産）サマリ `GET /api/assets/summary`
- [x] F-9: 学習ガイド `/learning`（静的 Markdown 5 トピック + `lib/markdown.ts` 自前実装 + 既読管理）
- [x] F-10: ダッシュボードのステップ進捗チェックリスト（`GET /api/onboarding/steps` + `lib/step-checklist.ts`）
- [x] F-11: 新規ユーザーの初期 viewMode を `household` に変更（Web/モバイル）
- [x] S-5: ログイン列挙対策と応答の均一化（`DUMMY_PASSWORD_HASH` + 統一メッセージ）
- [x] S-6: パスワードハッシュの非同期化とバージョン付き形式（`scrypt$N$r$p$salt$hash` + 旧形式透過再ハッシュ）
- [x] S-7: TOTP リプレイ防止（`totpLastUsedStep` + `verifyTotpWithReplayGuard()`）
- [x] S-8: 証憑アップロードの検証強化（マジックバイト + テナント別保存先 + 証憑削除 API）
- [x] S-9: レート制限（`lib/rate-limit.ts`。login=IP単位・import/sync=ユーザー単位）
- [x] S-14: モバイルセッションの SecureStore 永続化（`restoreSession()` + 起動時の有効性確認）
- [x] S-15: MFA コード入力 UI（Web/モバイルとも 2 段階目の入力画面を追加）
- [x] S-10: tenant-db ホワイトリストの DMMF 自動導出（登録漏れを構造的に排除）
- [x] S-11: CSV 取込の堅牢化（行数/サイズ上限・N+1 解消・全体原子化）
- [x] S-12: 監査ログ強化（tenantId/ip/userAgent + redactSecrets + 記録必須イベント）

---

## 1. 機能トラック（仕様書 §6・付録 A）

### F-1【P1・小】収支マイナス警告（ステップ 1）✅ 実装済み

- 参照: 仕様書 §6.1【追加設計】
- 変更対象: `src/app/dashboard/page.tsx`（および KPI カード・予実対比コンポーネント）
- 実装: `GET /api/kpi` の `operatingProfit < 0` で KPI「貯蓄額」カードを赤色強調 + 警告メッセージ表示。予実対比の月次テーブル・グラフで実績支出合計 > 実績収入合計の月を赤背景に。**API 変更は不要（画面のみ）**
- 受け入れ条件: 赤字データの seed で警告が表示され、黒字時は表示されない（Playwright E2E を 1 本追加）— `e2e/deficit-warning.spec.ts` にて実サーバー+DB で確認済み

### F-2【P1・中】配分提案 API `GET /api/budgets/allocation-suggest` ✅ 実装済み

- 参照: 仕様書 §6.2【追加設計】、詳細設計書 §9.1
- 変更対象: 新規 `src/lib/allocation.ts`（純関数 `suggestAllocation()`）+ 新規ルート `src/app/api/budgets/allocation-suggest/route.ts`
- 実装: `?year=&month=&basis=budget|actual` で当月の収入合計（REVENUE 科目の予算 or 実績）を基準額とし、`allocation_rules`（実装済み）の % からルール別推奨額（min/max/recommended、銭単位 ROUND_HALF_UP）を返す。`lib/budget-overlay.ts` のオーバーレイ額を差し引いた「配分可能額」も返す。role: viewer
- 受け入れ条件: `allocation.test.ts` で丸め・上限なしルール・オーバーレイ控除・収入 0 の各ケースを検証 — 7 件のテストで実装済み。実サーバー+DB で `GET /api/budgets/allocation-suggest` のオーバーレイ控除・50/30/20 集計を curl で確認済み

### F-3【P1・中】配分適用 API + 画面（Web 配分提案タブ / モバイル「予算に反映」） ✅ 実装済み

- 参照: 仕様書 §6.2【追加設計】、詳細設計書 §9.1
- 依存: F-2
- 変更対象: 新規 `src/app/api/budgets/allocation-apply/route.ts`、`src/app/budget/page.tsx`（配分提案タブ）、`src/app/settings/page.tsx`（配分ルールタブ）、モバイル `RevenueAllocationModal.tsx`
- 実装: `POST /api/budgets/allocation-apply`（editor）は `{ items: [{ accountId, month, amount }] }`（最大 200 件）を受け、**全 accountId の自テナント所有を事前検証**（不一致は 404）→ `$transaction` で `budgets` へ upsert → `audit("allocation_apply", ...)`。Web は収入合計 → ルール別入力欄 → 合計超過警告 → 一括登録（配分提案タブ）+ 予算配分ルールタブ（設定画面、科目紐付け編集）。モバイルは modal に「◯月の予算に反映」ボタン
- 受け入れ条件: 他テナント accountId 混入で 404・一切書き込まれないこと（`tenant-isolation.integration.test.ts` に追加、実 DB で 2 件パス）。Web は Playwright + psql で「配分提案タブ→一括反映クリック→budgets への実書き込み」を実サーバーで確認済み

### F-4【P1・小〜中】モード別表示名の全画面反映（ステップ 6）✅ 実装済み

- 参照: 仕様書 §6.6【追加設計】、詳細設計書 §9.5
- 変更対象: 新規 `src/lib/display-name.ts` + 科目名を表示する全画面（dashboard / budget / entry / assets / reports / closing / journals のセレクタ・テーブル・凡例）。モバイルは同シグネチャの関数を `app/mobile/src/` に複製
- 実装: `displayName(account, viewMode)` — household→`name` / sole→`soleName ?? name` / corporate→`corporateName ?? name`。**サーバー変更不要**（`GET /api/accounts` は両名を返却済み）
- 受け入れ条件: `display-name.test.ts`（null フォールバック含む）+ モード切替で科目名が変わる E2E 1 本 — unit test 完備。E2E は実サーバー+DB で `PUT /api/accounts/display-names` により一時的に表示名を設定し目視確認（デモテナントの seed データには sole/corporateName が未設定のため、既存の Playwright E2E スイートには追加せず、検証記録として本書に残す）
- 既知の制約: デモ seed（`prisma/seed.ts`）の勘定科目は `soleName`/`corporateName` が未設定。実運用では `PUT /api/accounts/display-names` で設定する運用を想定

### F-5【P2・中〜大】明細と科目の紐付け・実績転記（ステップ 3）✅ 実装済み（linkedAccountId は未実装）

- 参照: 仕様書 §6.3【追加設計】、詳細設計書 §9.2
- 変更対象: schema（`BankTransaction` に `categoryAccountId?` / `postedRecordId? @unique` 追加、新モデル `TxnCategoryRule`）+ マイグレーション + 新規 `PATCH /api/bank-transactions/[id]/categorize` + `lib/banktxn-import.ts`（取込時自動分類）+ `/bank-transactions` 画面
- 実装: 詳細設計書 §9.2 のトランザクション手順に従う。**二重転記は `postedRecordId` の `@unique` で DB 層でも防止**（`postedRecordId: null` 条件付き `updateMany` で 0 件更新を検知し `$transaction` をロールバック）。自動分類は `classifyByRules()`（優先度降順→キーワード長い順）で categoryAccountId を埋めるだけで転記しない。`TxnCategoryRule` は `TENANT_SCOPED_MODELS` へ登録
- 受け入れ条件: 並行 2 リクエストで転記が 1 回だけ成功（409）/ 他テナント明細・科目は 404 / 自動分類の優先順位テスト — `tenant-isolation.integration.test.ts` に 5 件追加（実 DB で並行 2 リクエストのテストを含めパス）、`banktxn-import.test.ts` に `classifyByRules()` の優先度・キーワード長・非マッチの各ケース。実サーバー+DB で紐付け→転記→学習→CSV 再取込時の自動分類→二重転記拒否の一連の流れと Web 画面表示を確認済み
- 既知の簡略化: 詳細設計書が挙げる `linkedAccountId`（クレジットカード明細用、`BankAccount` と排他）は見送った。現行コードにカード明細取込の実装・データが存在せず、既存の `accountId`（BankAccount 必須）を nullable 化する広範な変更を正当化できないため。カード明細を扱う機能が具体化した時点で改めて設計する

### F-6【P2・中】月間実績フロー図 `GET /api/cashflow/monthly` ✅ 実装済み

- 参照: 仕様書 §6.3【追加設計】、詳細設計書 §9.2
- 依存: F-5
- 変更対象: 新規 `src/lib/cashflow-monthly.ts`（純関数）+ 新規ルート + `/bank-transactions` フロー図タブに月選択
- 実装: 紐付け済み明細 + transfers から「収入源 → 口座 → カード/引落 → 支払項目」の Sankey ノード/リンクを生成。既存 `lib/transferflow.ts` のノード命名規約を踏襲（`externalKey()` を export して再利用）。フロー図タブに「設定ベース / 実績ベース（月次）」の切替＋年月セレクタを追加
- 受け入れ条件: `cashflow-monthly.test.ts`（未紐付け明細の除外・ノード重複排除）— 5 件のテストで実装済み。実サーバー+DB で、資金移動ルールのみ／明細を科目紐付けした場合の両方で同一口座ノードが重複せず統合されることを確認済み

### F-7【P2・中】元利均等返済の自動計算とオーバーレイ全ローン対応（ステップ 5）✅ 実装済み（number ベース、Decimal 演算は不採用）

- 参照: 仕様書 §6.5【追加設計】、詳細設計書 §9.4
- 変更対象: `src/lib/debt-schedule.ts`（`annuityMonthlyPayment()` / `amortizationSchedule()` 追加）+ `lib/budget-overlay.ts`（`computeHousingLoanOverlay` → 全ローン対応の `computeLoanOverlay` へ拡張・改称、旧名はエイリアスとして残存）+ `/loans` 残高グラフ
- 実装: 月額 = P×r(1+r)^n/((1+r)^n−1)。最終回で丸め誤差清算（Σ元本=P を厳密保証）。`monthlyPayment` 入力済みは入力値優先、未入力は計算値。レスポンスに由来 `source: "manual"|"annuity"` を含める。既存レスポンスキー `housingLoanOverlay` は同一データ（全ローン）で 1 リリース併存。**Decimal 演算は不採用**: 既存 `computeDebtSchedule` が number ベースのため実装方針を統一し、decimal.js 等の新規依存を避けた。`/loans` 残高グラフは今日以降を `amortizationSchedule()` の `remaining` 系列に置換（線形補間を廃止）。ブラウザのタイムゾーンによる月境界ズレを避けるため `Date.UTC` で構築した日付を渡す
- 受け入れ条件: `debt-schedule.test.ts` に r=0 / 1ヶ月 / 最終回清算 / 入力値優先の各ケース — 8 件のテストで実装済み。実サーバー+DB で、`linkedAccountId` 設定済み・`monthlyPayment` 未入力の事業性ローンを作成し `source: "annuity"` での自動計算・住宅ローン（`source: "manual"`）との共存・旧キー `housingLoanOverlay` の後方互換を確認済み。Playwright で残高推移グラフの表示も確認済み

### F-8【P2・小〜中】資産カテゴリ拡張と総資産サマリ（ステップ 4）✅ 実装済み

- 参照: 仕様書 §6.4【追加設計】、詳細設計書 §9.3
- 変更対象: schema（`PersonalAssetCategory` enum に `CASH` / `DEPOSIT` / `SECURITIES` を追加。実体は Prisma enum のため `ALTER TYPE ... ADD VALUE` によるマイグレーションが必要 — 当初「String のため定数追加のみ」としていたのは誤りで、実装時に schema を確認して訂正した）+ 新規 `src/lib/asset-summary.ts`（純関数 `summarizeNetWorth()`）+ 新規 `GET /api/assets/summary` + `/assets` サマリカード（`NetWorthSummaryCard`）
- 実装: 総資産 = 実物資産評価額（`isCountedAsAsset()` フィルタ後）+ 銀行口座残高（`BankTransaction` 明細合計）+ ASSET 科目残高。総負債 = LIABILITY 科目残高 + ローン残高 + 実物資産紐付け負債残高（`computeDebtSchedule()`）。**二重計上の除外規則**を純関数内で実施: ① `linked_accounts.accountId` に紐付く ASSET 科目残高は銀行口座残高側で計上済みのため除外、② `personal_assets.linkedAccountId` に紐付く LIABILITY 科目残高は実物資産紐付け負債側で計上済みのため除外。`LinkedAccount.accountId`（→ `Account.id`）と銀行口座残高側の `BankAccount.id` は別 ID 空間である点に注意して実装。Redis キャッシュ `assets:summary:{tenantId}:{year}-{month}`（1 時間）・`personal-assets` / `loans` / `bank-accounts` の各書き込み API 成功時に `invalidateCache()` で無効化
- 受け入れ条件: `asset-summary.test.ts`（8 件）で除外規則ごとの検証（ASSET 側除外/非除外、LIABILITY 側除外/非除外、`isCountedAsAsset` フィルタ、ローン単独集計、内訳・純資産の総合計算、全ゼロ入力）が成功。実サーバー+DB で、`LinkedAccount` 紐付き ASSET 科目・`PersonalAsset.linkedAccountId` 紐付き LIABILITY 科目それぞれについて curl で除外を確認済み（検証後にテストデータは削除済み）。Playwright で `/assets` の総資産サマリカード表示を確認済み
- 既知の制約: `lib/redis.ts` の `getRedisClient()` はコネクション確立を待たずに `isReady` を同期チェックするため、コールドスタート直後の初回 `invalidateCache()` が無音で no-op になるレース条件が既存する（本タスクで新規に導入したものではなく、キャッシュを使う全機能に共通する既存の制約）。2 回目以降の呼び出しでは正しく動作するため実害は限定的だが、修正するには `lib/redis.ts` の接続処理を async 化し全呼び出し元を見直す必要があり、本タスクのスコープ外とした

### F-9【P3・大】学習ガイド `/learning`（ステップ 6）✅ 実装済み

- 参照: 仕様書 §6.6【追加設計】、詳細設計書 §9.5
- 変更対象: `src/content/learning/*.md`（5 トピック: 家計と法人会計の対応・複式簿記の基礎・決算書の読み方・決算処理の流れ・必要申告書ガイド）+ `GET /api/learning/topics(/[key])` + `POST /api/learning/progress` + 新画面 `/learning`（sole/corporate ナビに追加）+ `learning_progress` テーブル（userId 所有。tenantId 列は持つが `TENANT_SCOPE_EXCLUDED_MODELS` に理由コメント付きで登録し自動テナントスコープの対象外とした。アクセス制御は常に `userId: user.id` で明示的に絞り込む）
- 実装: **`key` は `TOPIC_KEYS`（ビルド時に確定する定数配列）による `z.enum` のホワイトリストで検証し、ファイルパスをリクエスト値から組み立てない**（パストラバーサルの構造的排除）。Markdown → HTML 変換は新規依存を追加せず `lib/markdown.ts` に自前実装: 生テキストを先に HTML エスケープしてから見出し/太字/斜体/インラインコード/箇条書き/リンクの変換規則を適用し、リンクは `https://` または `/` 始まりのみ許可（サニタイズを構造的に保証）
- 受け入れ条件: `markdown.test.ts`（10 件。生 HTML・属性注入・危険スキームのリンクを含む）・`learning-content.test.ts`（4 件）で検証。実サーバー+DB で、パストラバーサル文字列を含む不正 key が 400 になること、`POST /api/learning/progress` の既読登録が `userId` 単位で分離され他ユーザー（viewer）から見えないことを curl で確認済み。Playwright で `/learning` のトピック一覧・本文表示・既読バッジの切り替えを確認済み
- 既知の簡略化: 「科目対応表の教材化」（`/account-conversion` への解説オーバーレイ）と各画面のコンテキストヘルプ（❓アイコン）は本タスクのスコープ外とした（学習ガイド本体と既読管理という受け入れ条件の中核を優先）

### F-10【P3・小】ステップ進捗チェックリスト ✅ 実装済み

- 参照: 仕様書 付録 A
- 変更対象: `/dashboard`（`StepChecklistCard`）+ 新規 `GET /api/onboarding/steps` + 新規 `lib/step-checklist.ts`（純関数 `computeStepChecklist()`）+ `lib/use-view-mode.ts`（`hasSwitchedViewMode()`）+ `AppShell.tsx`（`changeViewMode()` でモード切替を localStorage に記録）
- 実装: ステップ 1〜6 の達成状況（収入予算あり / 支払予算あり / 口座あり / 実物資産あり / ローンあり / モード切替経験）をチェックリスト表示。前者 5 項目は `GET /api/onboarding/steps` が DB から判定（Budget の科目カテゴリ・BankAccount/PersonalAsset/Loan の存在有無）。「モード切替経験」のみ client 専用の事実として localStorage の `viewmode-switched` フラグで判定（サーバー側で追跡する必要がないため）。全 6 ステップ達成後はカードを自動的に非表示にする（オンボーディング用途）
- 受け入れ条件: `step-checklist.test.ts`（3 件）で `computeStepChecklist()` の入出力を検証。実サーバー+DB+Playwright で、5/6 達成状態でカードが表示されること、法人モードへの切替後に 6/6 となりカードが自動的に消えることを画面で確認済み

### F-11【P3・小】初期 viewMode を `household` に変更 ✅ 実装済み

- 参照: 仕様書 付録 A、§5
- 変更対象: `src/components/AppShell.tsx`（`useState` 初期値）+ `lib/use-view-mode.ts`（`useViewMode()` のデフォルト引数）+ モバイル `api.ts`（`_viewMode` 初期値）
- 実装: 新規ユーザー（Web: localStorage 未設定 / モバイル: メモリ内保持のため起動直後は常に該当）の初期値を `sole` → `household` へ。Web は既存ユーザーの保存済み `localStorage` の値を `useEffect` で上書きするため、既存ユーザーの挙動は変更しない
- 受け入れ条件: 実サーバーで localStorage 未設定のブラウザから `/dashboard` を開き、家計モード（ナビ・KPI ラベルとも家計表記）で初期表示されることを Playwright で確認済み。Web/モバイルとも typecheck 成功

---

## 2. セキュリティトラック（詳細設計書 §2〜§8）

> 実装順は S-1 → S-4 → S-2 → S-3 を推奨（悪用容易度順）。各タスクの詳細仕様・移行手順は詳細設計書の該当節を必ず読むこと。

### S-1【P1・小】証憑配信の認可（詳細設計書 §5.1）✅ 実装済み

- 変更対象: `src/app/api/uploads/[filename]/route.ts`、schema（`Receipt.savedName` / `mimeType` 追加 + `fileUrl` からのデータ移行 SQL）
- 実装: **現状は無認証**。`withApi({ role: "viewer" })` 化し、`receipt.findFirst({ where: { savedName, journalEntry: { tenantId } } })` で所有確認（他テナントは 404）。`Content-Disposition: attachment` / `X-Content-Type-Options: nosniff` / `Cache-Control: private, no-store` を付与し、Content-Type は保存時検証値を使う
- 受け入れ条件: 未ログイン 401 / 他テナント receipt 404 のテスト

### S-2【P1・中】MFA の 2 段階化と総当たり対策（詳細設計書 §3.1） ✅ 実装済み

- 変更対象: `src/app/api/auth/login/route.ts`、新規 `POST /api/auth/mfa/verify`、`prisma`（`mfa_challenges` または Redis）
- 実装: `mfa_challenges` テーブル（token は SHA-256 ハッシュ・TTL 5 分）を使う 2 段階フローへ移行。`lib/mfa-challenge.ts` に `issueMfaChallenge()`/`consumeMfaChallenge()` を切り出し、`login/route.ts`（旧互換の password+code 同時送信）と新規 `POST /api/auth/mfa/verify` の両方から共用。MFA 失敗 5 回でチャレンジ失効 + アカウントロック（15分）。`loginAttempts` のリセットはセッション発行時へ移動。`/api/admin/cleanup` に期限切れチャレンジの削除を追加
- 受け入れ条件: mfaToken の TTL・使い捨て・5 回失効 / 旧フロー互換のテスト — `mfa-challenge.integration.test.ts`（実 DB、4 件）+ 実サーバーへの curl で 2 段階フロー・トークン再利用拒否・5 回失敗ロック・旧互換フローを確認済み
- 既知の制約（未実装）: Web `/login`・モバイル `LoginScreen` に MFA コード入力 UI が無いため、MFA 有効ユーザーは現状 API 経由でのみ第 2 段階を完了できる（バックエンドの 2 段階化自体は本タスクで完了）。UI 追加は別タスクとして今後切り出すこと

### S-3【P1・中】セッションのハッシュ保存・二重期限・Bearer ログアウト修正（詳細設計書 §2.3） ✅ 実装済み

- 変更対象: `src/lib/auth.ts`、schema（`Session` に `lastSeenAt` / `ip` / `userAgent`）、`middleware.ts`（Cookie 名定数の共有）
- 実装: トークンは `fm2:` プレフィックス付き乱数を発行し DB には SHA-256 のみ保存（旧平文セッションは 7 日で自然失効）。アイドル 24h + 絶対 7 日。ログイン毎ローテーション。**`destroySession()` を Cookie / Bearer 両対応に修正**（現状モバイルはログアウト不能）。パスワード・ロール変更時の全セッション失効。Cookie は `__Host-` プレフィックス
- 受け入れ条件: 旧→新トークン併存 / Bearer ログアウト / ローテーションのテスト — next/headers の `cookies()`/`headers()` はリクエストスコープ外から呼べないため（実測で確認）、単体テストではなく実サーバー + 実 DB で curl/psql により全項目を確認: `fm2:` トークン発行と DB 側 SHA-256 ハッシュの一致、Bearer ログアウトでの実削除、旧形式（平文）セッションの継続動作、アイドル/絶対期限それぞれ単独での失効、ログイン毎のローテーション（2 セッション同時有効）、ロール変更時の対象セッション失効。既存 F-1 E2E も再確認済み

### S-4【P1・小〜中】CSRF 防御とセキュリティヘッダー（詳細設計書 §4.1・§4.2）✅ 実装済み

- 変更対象: `src/middleware.ts`、`next.config.ts`
- 実装: `/api/*` の POST/PUT/PATCH/DELETE で Origin / Sec-Fetch-Site を検証（Bearer 認証は免除、判定不能な Cookie リクエストは 403）。`headers()` で CSP（まず Report-Only で導入）・HSTS・nosniff・frame-ancestors を全ルートに付与
- 受け入れ条件: middleware 単体テスト（Origin 不一致 403 / Bearer 免除）+ E2E でヘッダー存在確認

### S-5【P2・小】ログイン列挙対策と応答の均一化（詳細設計書 §2.2）✅ 実装済み

- 変更対象: `src/app/api/auth/login/route.ts` + `lib/auth.ts`（`DUMMY_PASSWORD_HASH` 追加）
- 実装: ユーザー不在時もダミーハッシュに対し `verifyPassword` を必ず実行。失敗メッセージを「メールアドレスまたはパスワードが正しくありません」に統一し、**「残り N 回でロック」の開示をやめる**（残回数は監査ログのみ）
- 受け入れ条件: `auth.test.ts`（4 件）で `hashPassword`/`verifyPassword`/`DUMMY_PASSWORD_HASH` を検証。実サーバー+DB で、存在しないメールアドレスと実在ユーザーの誤パスワードが同一メッセージを返すこと・5 回連続失敗で 429 ロックに遷移すること・監査ログには正確な試行回数が記録されレスポンスには含まれないことを curl + psql で確認済み

### S-6【P2・小〜中】パスワードハッシュの非同期化とバージョン付き形式（詳細設計書 §2.1）✅ 実装済み

- 変更対象: `src/lib/auth.ts`（呼び出し元 3 箇所を async 化: `api/auth/login`・`api/admin/users`・`api/admin/users/[id]`）
- 実装: `scrypt$N$r$p$salt$hash` 形式（N=2^15=32768、旧デフォルトの倍のコスト）+ 非同期 `crypto.scrypt`（`util.promisify`）。旧 `salt:hash` 形式（固定 N=16384 r=8 p=1）は `isLegacyPasswordHash()` で判定でき、`verifyPassword()` は両形式に対応。ログイン成功時に旧形式なら透過再ハッシュ。S-5 の `DUMMY_PASSWORD_HASH` も新形式で再生成
- 受け入れ条件: `auth.test.ts`（9 件。新形式往復・旧形式後方互換・不正フォーマット拒否等）で検証。実サーバー+DB で、旧形式ハッシュを持つ既存デモユーザーのログイン成功時に `passwordHash` が新形式へ書き換わること・2 回目以降のログインが新形式検証パスで成功すること・管理者 API 経由の新規作成/パスワードリセットがいずれも新形式ハッシュを生成しログインに成功することを curl + psql で確認済み

### S-7【P2・小】TOTP リプレイ防止（詳細設計書 §3.2）✅ 実装済み

- 変更対象: `src/lib/totp.ts`（`verifyTotpStep()` 追加）+ 新規 `src/lib/totp-replay-guard.ts`（`verifyTotpWithReplayGuard()`）+ schema（`User.totpLastUsedStep BigInt?`）+ `lib/mfa-challenge.ts` / `api/auth/mfa/{enable,disable,recovery}/route.ts`（TOTP 検証 4 箇所を統一）
- 実装: 検証成功時に使用ステップを記録し、記録値以下のコードを拒否
- 受け入れ条件: `totp.test.ts` に `verifyTotpStep()` の往復・null・同一コードで同一ステップを返すことの 3 件を追加。実サーバー+DB で、`/mfa/enable` で使用したコードを直後に `/mfa/disable` へ再送すると拒否される（cross-endpoint でのリプレイ検出）こと、ログイン 2 段階フローで同一コードを 2 回のチャレンジに使い回すと 2 回目が拒否されること、次の 30 秒ステップの新しいコードでは正しくログインできることを確認済み

### S-8【P2・小〜中】証憑アップロードの検証（詳細設計書 §5.2）✅ 実装済み

- 依存: S-1
- 変更対象: `src/lib/upload.ts`（`matchesMagicBytes()` / `tenantUploadDir()` 追加）、`journals/[id]/receipts/route.ts`、`uploads/[filename]/route.ts`、新規 `journals/[id]/receipts/[receiptId]/route.ts`
- 実装: サイズ上限 10MB（既存実装済み）/ 拡張子ホワイトリスト（既存実装済み）/ マジックバイト検証（新規）。保存先を `uploads/<tenantId>/` に分離（旧フラット配置ファイルは配信時にフォールバック参照）。証憑削除 API（`DELETE .../receipts/[receiptId]`）追加。副次的に、巨大ファイルで `req.formData()` がパース例外を投げ 500 になっていたバグを 400 に修正
- 受け入れ条件: `upload.test.ts` に `matchesMagicBytes()` の 4 件を追加。実サーバー+DB で、正しい PDF のアップロード成功・HTML を `.pdf` と詐称したアップロードの 400 拒否・11MB ファイルの 400 拒否（修正前は 500）・保存先のテナント別分離・新旧両配置ファイルのダウンロード・他テナントからの削除/ダウンロードの 404 拒否・削除時の DB レコードとファイル両方の消去を curl + psql + ファイルシステムで確認済み

### S-9【P2・中】レート制限（詳細設計書 §4.3）✅ 実装済み

- 変更対象: 新規 `src/lib/rate-limit.ts`、login / mfa / import / sync 各ルート
- 実装: Redis INCR+EXPIRE の固定窓。`POST /api/auth/login` は IP 単位 10 回/5 分（`rl:login:ip:<ip>`）。`POST /api/financials/import`・`/api/budgets/import`・`/api/bank-accounts/[id]/transactions`（CSV 取込のみ）・`/api/bank-accounts/[id]/sync` はユーザー単位 10 回/10 分（`rl:{import,sync}:user:<userId>`）。超過は 429 + Retry-After。Redis 不通時は認証系（login）のみメモリ内フォールバックで制限を維持し、import/sync は fail-open（可用性優先）。`POST /api/auth/mfa/verify` の `rl:mfa:token:<sha256(mfaToken)>`（5 回/トークン生存中）は S-2 で実装済みの `MfaChallenge.attempts` + `MAX_MFA_ATTEMPTS` と同一の仕組みのため新規実装は不要と判断し、コメントで明記した
- 受け入れ条件: `rate-limit.test.ts`（3 件。`clientIp()` の抽出・空白除去・フォールバック）で検証。実サーバー+Redis で、login への同一 IP 連続アクセスが 10 回まで通り 11 回目が 429（Retry-After 付き）になること・別 IP は影響を受けないこと・Redis 停止中もメモリフォールバックで同じ挙動を維持すること・import 系も同様に 10 回で 429 になること・Redis 停止中は import 系が fail-open で正常動作することを curl + redis-cli で確認済み

### S-10【P3・小】tenant-db ホワイトリストの DMMF 自動導出（詳細設計書 §6.1）✅ 実装済み

- 変更対象: `src/lib/tenant-db.ts`
- 実装: 手書き Set を `Prisma.dmmf` から「tenantId 保有モデル − EXCLUDED」で起動時構築へ置換（回帰テストは実装済みなので挙動同値を確認するだけでよい）
- 受け入れ条件: 導出結果が旧ハードコードの 31 モデルと完全一致することを確認。既存の `tenant-db.test.ts`（3 件）・全 198 件のユニットテスト・統合テスト 24 件・build が成功。実サーバー+DB で `GET /api/accounts` が自テナント分のみ返すことを確認し、挙動が変わっていないことを検証済み

### S-11【P3・中】CSV 取込の堅牢化（詳細設計書 §7）✅ 実装済み

- 変更対象: `src/lib/import.ts`、`src/lib/bank-transactions.ts`（`upsertExternalTransactions`）、`api/financials/import`・`api/bank-accounts/[id]/transactions` の各ルート
- 実装: 行数上限 10,000 / ファイル 5MB（Content-Length 事前拒否）/ 科目・期間の一括解決（N+1 解消。`findMany` + `createManyAndReturn`）/ `$transaction` で全体原子化（エラー行があれば全体中止に**仕様変更**。207 だったエラー時レスポンスも 400 に変更）。銀行明細 CSV（`upsertExternalTransactions`）も 1 件ずつの `upsert` 直列実行を `createMany({ skipDuplicates: true })` に置換し同じ上限を適用
- 受け入れ条件: 実サーバー+DB で、有効行の一括取込・不正科目コード混在時の all-or-nothing 中止・10,001 行での 400・5MB 超ファイルの Content-Length 時点での 400・銀行明細の重複スキップ（`skipDuplicates`）・銀行明細 CSV の行数上限適用を curl + psql で確認済み

### S-12【P3・小〜中】監査ログ強化（詳細設計書 §8）✅ 実装済み

- 変更対象: `src/lib/audit.ts`、schema（`AuditLog` に `tenantId` / `ip` / `userAgent`）、`lib/api-handler.ts`（`ctx.audit` への自動付与）、`lib/mfa-challenge.ts`・`api/auth/login`・`api/auth/mfa/verify`・`api/admin/users/[id]`・`api/uploads/[filename]`
- 実装: `redactSecrets()` で passwordHash / totpSecret / mfaRecoveryCodes をマスク。書き込み失敗は `console.error` で観測可能に。記録必須イベント（`rate_limited(login)` / `account_locked` / `mfa_verify_failed` / `session_revoked_all` / `receipt_download`）を追加
- 受け入れ条件: `audit.test.ts`（4 件）で `redactSecrets()` を検証。実サーバー+DB で、通常ログインの tenantId/userId/ip/userAgent 記録、5 回連続失敗での `account_locked` 記録、レート制限超過での `rate_limited` 記録、パスワードリセットでの `session_revoked_all` 記録、証憑ダウンロードでの `receipt_download` 記録を curl + psql で確認済み

### S-13【P3・小】環境変数の起動時検証とサービスキー定数時間比較（詳細設計書 §1.3・§4.4）

- 変更対象: 新規 `src/lib/config.ts`、`admin/cleanup/route.ts`
- 実装: Zod で環境変数を起動時検証（`CLEANUP_SERVICE_KEY` は 32 文字未満拒否）。cleanup のキー比較を `timingSafeEqual` 化

### S-14【P2・小】モバイルセッションの SecureStore 永続化（詳細設計書 §2.4）✅ 実装済み

- 変更対象: `app/mobile/src/api.ts`、`App.tsx`（依存追加: `expo-secure-store`〜15.0.8 — 本タスクのみ依存追加を許可）
- 実装: セッショントークンを SecureStore に保存し、起動時に `GET /api/auth/me` で有効性確認。AsyncStorage は使わない。無効なトークンは SecureStore からも削除、ネットワークエラー時はトークンを保持し次回起動で再試行する
- 受け入れ条件: mobile の typecheck 成功。実サーバーで `GET /api/auth/me` が有効な Bearer トークンで 200、無効なトークンで 401 + `user:null` を返すことを curl で確認し、`restoreSession()` のロジックと整合することを検証済み（実機/シミュレータでの動作確認はサンドボックス環境の制約により未実施）

### S-15【P2・小〜中】MFA コード入力 UI（Web / モバイル）✅ 実装済み

- 参照: S-2（バックエンドの 2 段階化は実装済み）
- 変更対象: `src/app/login/page.tsx`（新規: MFA コード入力ステップ）、モバイル `app/mobile/src/screens/LoginScreen.tsx` + `api.ts`（`login()` を判別共用体化 + 新規 `verifyMfa()`）
- 実装: `POST /api/auth/login` が `{ mfaRequired: true, mfaToken }` を返した場合に、TOTP コード（6桁）またはリカバリーコード入力欄を表示し `POST /api/auth/mfa/verify` を呼ぶ 2 画面目を追加する。ロック時（429）のメッセージ表示も含める
- 受け入れ条件: Web/モバイルとも typecheck・既存テストが成功。実サーバー+DB で MFA 有効ユーザーの TOTP シークレットを実際に発行・有効化し、Playwright で (1) ログイン後に 2段階認証画面へ遷移すること (2) 誤ったコードでエラー表示・コード欄クリアされること (3) 正しい TOTP コードでダッシュボードまで到達することを画面付きで確認済み
- 受け入れ条件: MFA 有効ユーザーが Web / モバイルの UI 操作のみでログインを完了できる E2E（またはモバイルは手動確認）

---

## 3. タスク間の依存関係

```
F-2 ──→ F-3          F-5 ──→ F-6          S-1 ──→ S-8
（配分提案 → 適用）   （紐付け → 月間フロー）（配信認可 → 取込検証）

S-2・S-3 は同時期に実施すると login/route.ts の競合が起きやすい。S-3 → S-2 の順を推奨。
```

## 4. 完了報告のフォーマット

各タスク完了時、コミットメッセージに `[F-x]` / `[S-x]` の ID を含め、以下を報告すること。

1. 変更ファイル一覧と要約
2. 追加・更新したテストと実行結果（typecheck / test / build のログ末尾）
3. `再設計仕様書.md`・本書の更新箇所
4. 仕様と実装が乖離した点（あれば理由付き）
