# app/mobile

決算管理システムの **モバイルアプリ**（Expo / React Native）。
Web と共通のバックエンド API (`/api/*`) を参照する。

## 開発
```bash
npm install
npm run start    # Expo Dev Server
```
API のベース URL は `app.json` の `extra.apiBaseUrl` で設定する。
