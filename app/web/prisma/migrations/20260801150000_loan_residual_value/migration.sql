-- 残価設定ローン（カーローン等）への対応。
--
-- 残価設定型クレジットは最終回に据置額（残価）を一括で支払う（または車両返却／再ローン）契約で、
-- 毎月の返済は残価を除いた分だけを償却する。従来の償還計算は最終回で必ず残高がゼロになる
-- 前提だったため、月額が実際より高く、残高の減りも速く出ていた。
-- residualValue を持たせ、月額算出・償還表・残高計算のすべてで最終回に残価を残すようにする。

-- AlterTable
ALTER TABLE "loans" ADD COLUMN "residualValue" DECIMAL(18,2);
